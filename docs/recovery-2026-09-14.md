# Phase 4B recovery and branding reconciliation

14 September 2026. The interrupted run stopped before committing its proposed merge tree. Remote model-control/history work remained on `phase4b-model-controls` at `c0b0984`; main's separate HermesUI NG branding work remained at `6f42dc1`. No source was recovered by overwriting either branch.

This merge preserves both parents and all individual implementation commits. The supplied Hermes mark replaces the old star in the welcome view, streaming response and newly extracted saved-message component. The diagnostic keeps both the HermesUI NG title and its new reasoning capability row. Existing branded-asset routing and branding tests remain unchanged; the structured-history browser regression additionally checks decorative Hermes marks and absence of the old star.

Fresh local verification with Node 22.16.0 and archived dependencies matching the exact lockfile: build, server/web typecheck, lint, 134 unit tests and 18 HTTP/WebSocket contracts passed. Final combined browser, Docker and vanilla-Hermes CI is required before merge into main; earlier standalone Phase 4B evidence is retained, not mislabelled as combined-build evidence.

The operator diagnostics report healthy REST/local access/ready Gateway but unimplemented model/profile controls. No private diagnostic or transcript is committed. Known upstream reasoning-scope race and outstanding M3/PWA/release gates remain as documented in the implementation status.
